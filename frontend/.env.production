# Production environment for Vercel build.
# Set REACT_APP_BACKEND_URL in the Vercel dashboard (Settings → Environment Variables)
# rather than committing it here, OR replace the placeholder below before deploying.
REACT_APP_BACKEND_URL=https://YOUR-RENDER-SERVICE.onrender.com
ENABLE_HEALTH_CHECK=false
